{
  "name": "cortex",
  "version": "0.1.10",
  "private": true,
  "description": "Cortex: memoria de contexto para proyectos software (documental + vectorial + grafo + bi-temporal), con MCP para agentes de IA",
  "type": "module",
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "admin": "node --conditions=development --import tsx apps/admin/src/index.ts",
    "build": "tsc -b tsconfig.build.json",
    "clean": "rm -rf packages/*/dist apps/*/dist packages/*/*.tsbuildinfo apps/*/*.tsbuildinfo",
    "cortex": "node --conditions=development --import tsx apps/cli/src/index.ts",
    "cortex:link": "node --conditions=development --import tsx apps/cli/src/index.ts link",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f postgres",
    "db:migrate": "pnpm --filter @cortex/database run migrate",
    "db:seed": "pnpm admin seed",
    "db:up": "docker compose up -d postgres",
    "mcp": "pnpm --filter @cortex/mcp-server run dev",
    "release:notes": "node scripts/changelog-notes.mjs",
    "test": "vitest run",
    "test:integration": "vitest run -c vitest.integration.config.ts",
    "test:watch": "vitest",
    "typecheck": "pnpm -r --if-present run typecheck",
    "version:set": "node scripts/set-version.mjs",
    "web": "pnpm --filter @cortex/web run dev"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4",
    "yaml": "^2.9.0",
    "zod": "^3.25.76"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Dinacode-Labs/cortex.git"
  }
}
