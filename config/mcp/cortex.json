{
  "//": "Definición canónica del servidor MCP de Cortex (compartida entre agentes).",
  "//install": "Claude Code: claude mcp add cortex -s user -- <command> <args...>. Codex: ~/.codex/config.toml [mcp_servers.cortex]. OpenCode: bloque mcp en su config. Ver config/README.md.",
  "//requires": "Postgres levantado (pnpm db:up) y .env configurado en el repo.",
  "mcpServers": {
    "cortex": {
      "command": "pnpm",
      "args": ["-C", "<ruta-al-repo-cortex>", "--filter", "@cortex/mcp-server", "start"]
    }
  }
}
