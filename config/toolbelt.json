{
  "$comment": "A THIRD-PARTY toolbelt registry. Cortex no longer distributes itself through here: its MCP, its skill and its command ship in the plugin (plugin/claude-code/) and `cortex setup` installs them. What is left is the schema for your organisation's registry — see docs/toolbelt-registry.md. The installer resolves {REPO}.",
  "mcpServers": {},
  "skills": [],
  "commands": []
}
